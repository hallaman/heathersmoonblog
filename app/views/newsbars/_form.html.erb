<%= simple_form_for(@newsbar) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :message, as: :summernote %>
    <%= f.input :show %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
