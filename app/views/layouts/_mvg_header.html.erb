<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
       <% if member_signed_in? && @issue.present? %>
        <%= link_to "MOON VIBE GUIDE", moonvibeguide_issues_path, :class => 'navbar-brand mvg-logo' %>
      <% elsif @issue.present? %>
        <%= link_to "MOON VIBE GUIDE", login_path, :class => 'navbar-brand mvg-logo' %>
      <% end %>
    </div>

    <div class="navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav navbar-left">
        <li><%= link_to image_tag("AN_Logo.png"), root_path, :class => 'logo text-center' %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if member_signed_in? && @issue.present? %>
          <li class="dropdown visible-xs">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">More <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <% @issue.sidebars.each do |item| %>
                <% if item.link.blank? %>
                  <li><%= link_to item.title, moonvibeguide_reading_path(item.podcast_link_id) %></li>
                <% else %>
                  <li><%= link_to item.title, item.link, target: '_blank' %></li>
                <% end %>
              <% end %>
            </ul>
          </li>
        <% end %>
        
      </ul>
          
      
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>