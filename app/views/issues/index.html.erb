<div class="container">

  <p id="notice"><%= notice %></p>

  <h1>Moon Vibe Guide ISsues</h1>

  <%# link_to 'New Issue', new_issue_path, class: 'btn btn-default' %>
  <br>
  <br>
  <table class="table">
    <thead>
      <tr>
        <th>Name</th>
       <!--  <th>Description</th>
        <th>Main image</th>
        <th>Location1</th>
        <th>Location2</th>
        <th>Location3</th> -->
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @issues.each do |issue| %>
        <tr>
          <td><%= issue.name %></td>
          <!-- <td><%= designer.description %></td>
          <td>  <%= image_tag(designer.main_image.url(:thumb), :class => "img-thumbnail") %></td>
          <td><%= designer.location1 %></td>
          <td><%= designer.location2 %></td>
          <td><%= designer.location3 %></td> -->
          <td><%= link_to 'Show', issue %></td>
          <td><%= link_to 'Edit', edit_issue_path(issue) %></td>
          <td><%= link_to 'Delete', issue, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>


</div>